Aim: Create a VPC and two EC2 with user data by using terraform 

#Files Used 
-provider.tf
-terraform.tfvars
-main.tf
-variables.tf
-output.tf

#terraform commands to compile the code
terraform init
terraform validate
terraform plan
terraform apply --auto-approve
terraform destroy --auto -approve
